export enum LogLevel {
  DEBUG = 'DEBUG',
  INFO = 'INFO',
  WARN = 'WARN',
  ERROR = 'ERROR',
}

export enum LogFormat {
  JSON = 'json',
  TEXT = 'text',
  STRUCTURED = 'structured',
}

export enum LogTarget {
  CONSOLE = 'console',
  FILE = 'file',
  CLOUD = 'cloud',
}
