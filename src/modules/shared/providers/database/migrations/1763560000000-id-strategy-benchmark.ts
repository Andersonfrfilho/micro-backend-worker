import { MigrationInterface, QueryRunner, Table, TableColumn, TableIndex } from 'typeorm';

/**
 * ID Strategy Benchmark Migration
 *
 * Cria 4 tabelas idênticas com estratégias de ID diferentes:
 * - benchmark_uuid_v7:   UUID v7 (sortable by timestamp)
 * - benchmark_uuid_v4:   UUID v4 (random)
 * - benchmark_nanoid:    Nanoid (string 21 chars)
 * - benchmark_snowflake: Snowflake/Discord (bigint)
 *
 * Objetivo: Comparar performance real em operações de INSERT/SELECT
 */
export default class IDStrategyBenchmark1763560000000 implements MigrationInterface {
  public async up(queryRunner: QueryRunner): Promise<void> {
    // ========================================================================
    // TABLE 1: UUID v7 (Sortable by timestamp)
    // ========================================================================
    await queryRunner.createTable(
      new Table({
        name: 'benchmark_uuid_v7',
        columns: [
          new TableColumn({
            name: 'id',
            type: 'uuid',
            isPrimary: true,
            default: 'uuidv7()',
            comment: 'UUID v7 generated by custom PostgreSQL function - sortable by timestamp',
          }),
          new TableColumn({
            name: 'name',
            type: 'varchar',
            length: '255',
            isNullable: false,
          }),
          new TableColumn({
            name: 'email',
            type: 'varchar',
            length: '255',
            isNullable: false,
          }),
          new TableColumn({
            name: 'age',
            type: 'integer',
            isNullable: false,
          }),
          new TableColumn({
            name: 'city',
            type: 'varchar',
            length: '100',
            isNullable: false,
          }),
          new TableColumn({
            name: 'data',
            type: 'jsonb',
            isNullable: true,
          }),
          new TableColumn({
            name: 'created_at',
            type: 'timestamp',
            default: 'now()',
          }),
          new TableColumn({
            name: 'updated_at',
            type: 'timestamp',
            isNullable: true,
          }),
        ],
      }),
    );

    // Índices para benchmark justo
    await queryRunner.createIndex(
      'benchmark_uuid_v7',
      new TableIndex({
        name: 'idx_uuid_v7_email',
        columnNames: ['email'],
      }),
    );

    await queryRunner.createIndex(
      'benchmark_uuid_v7',
      new TableIndex({
        name: 'idx_uuid_v7_city',
        columnNames: ['city'],
      }),
    );

    await queryRunner.createIndex(
      'benchmark_uuid_v7',
      new TableIndex({
        name: 'idx_uuid_v7_created_at',
        columnNames: ['created_at'],
      }),
    );

    // ========================================================================
    // TABLE 2: UUID v4 (Random - RFC 4122)
    // ========================================================================
    await queryRunner.createTable(
      new Table({
        name: 'benchmark_uuid_v4',
        columns: [
          new TableColumn({
            name: 'id',
            type: 'uuid',
            isPrimary: true,
            default: 'gen_random_uuid()',
            comment: 'UUID v4 (random) generated by PostgreSQL pgcrypto extension',
          }),
          new TableColumn({
            name: 'name',
            type: 'varchar',
            length: '255',
            isNullable: false,
          }),
          new TableColumn({
            name: 'email',
            type: 'varchar',
            length: '255',
            isNullable: false,
          }),
          new TableColumn({
            name: 'age',
            type: 'integer',
            isNullable: false,
          }),
          new TableColumn({
            name: 'city',
            type: 'varchar',
            length: '100',
            isNullable: false,
          }),
          new TableColumn({
            name: 'data',
            type: 'jsonb',
            isNullable: true,
          }),
          new TableColumn({
            name: 'created_at',
            type: 'timestamp',
            default: 'now()',
          }),
          new TableColumn({
            name: 'updated_at',
            type: 'timestamp',
            isNullable: true,
          }),
        ],
      }),
    );

    await queryRunner.createIndex(
      'benchmark_uuid_v4',
      new TableIndex({
        name: 'idx_uuid_v4_email',
        columnNames: ['email'],
      }),
    );

    await queryRunner.createIndex(
      'benchmark_uuid_v4',
      new TableIndex({
        name: 'idx_uuid_v4_city',
        columnNames: ['city'],
      }),
    );

    await queryRunner.createIndex(
      'benchmark_uuid_v4',
      new TableIndex({
        name: 'idx_uuid_v4_created_at',
        columnNames: ['created_at'],
      }),
    );

    // ========================================================================
    // TABLE 3: Nanoid (URL-safe string, 21 chars)
    // ========================================================================
    await queryRunner.createTable(
      new Table({
        name: 'benchmark_nanoid',
        columns: [
          new TableColumn({
            name: 'id',
            type: 'varchar',
            length: '21',
            isPrimary: true,
            isNullable: false,
          }),
          new TableColumn({
            name: 'name',
            type: 'varchar',
            length: '255',
            isNullable: false,
          }),
          new TableColumn({
            name: 'email',
            type: 'varchar',
            length: '255',
            isNullable: false,
          }),
          new TableColumn({
            name: 'age',
            type: 'integer',
            isNullable: false,
          }),
          new TableColumn({
            name: 'city',
            type: 'varchar',
            length: '100',
            isNullable: false,
          }),
          new TableColumn({
            name: 'data',
            type: 'jsonb',
            isNullable: true,
          }),
          new TableColumn({
            name: 'created_at',
            type: 'timestamp',
            default: 'now()',
          }),
          new TableColumn({
            name: 'updated_at',
            type: 'timestamp',
            isNullable: true,
          }),
        ],
      }),
    );

    await queryRunner.createIndex(
      'benchmark_nanoid',
      new TableIndex({
        name: 'idx_nanoid_email',
        columnNames: ['email'],
      }),
    );

    await queryRunner.createIndex(
      'benchmark_nanoid',
      new TableIndex({
        name: 'idx_nanoid_city',
        columnNames: ['city'],
      }),
    );

    await queryRunner.createIndex(
      'benchmark_nanoid',
      new TableIndex({
        name: 'idx_nanoid_created_at',
        columnNames: ['created_at'],
      }),
    );

    // ========================================================================
    // TABLE 4: Snowflake ID (Discord-style, bigint)
    // ========================================================================
    await queryRunner.createTable(
      new Table({
        name: 'benchmark_snowflake',
        columns: [
          new TableColumn({
            name: 'id',
            type: 'bigint',
            isPrimary: true,
            isNullable: false,
          }),
          new TableColumn({
            name: 'name',
            type: 'varchar',
            length: '255',
            isNullable: false,
          }),
          new TableColumn({
            name: 'email',
            type: 'varchar',
            length: '255',
            isNullable: false,
          }),
          new TableColumn({
            name: 'age',
            type: 'integer',
            isNullable: false,
          }),
          new TableColumn({
            name: 'city',
            type: 'varchar',
            length: '100',
            isNullable: false,
          }),
          new TableColumn({
            name: 'data',
            type: 'jsonb',
            isNullable: true,
          }),
          new TableColumn({
            name: 'created_at',
            type: 'timestamp',
            default: 'now()',
          }),
          new TableColumn({
            name: 'updated_at',
            type: 'timestamp',
            isNullable: true,
          }),
        ],
      }),
    );

    await queryRunner.createIndex(
      'benchmark_snowflake',
      new TableIndex({
        name: 'idx_snowflake_email',
        columnNames: ['email'],
      }),
    );

    await queryRunner.createIndex(
      'benchmark_snowflake',
      new TableIndex({
        name: 'idx_snowflake_city',
        columnNames: ['city'],
      }),
    );

    await queryRunner.createIndex(
      'benchmark_snowflake',
      new TableIndex({
        name: 'idx_snowflake_created_at',
        columnNames: ['created_at'],
      }),
    );
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.dropTable('benchmark_snowflake', true);
    await queryRunner.dropTable('benchmark_nanoid', true);
    await queryRunner.dropTable('benchmark_uuid_v4', true);
    await queryRunner.dropTable('benchmark_uuid_v7', true);
  }
}
